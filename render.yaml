services:
  - type: web
    name: blog-post-spa-backend
    env: node
    branch: main
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 3000
  - type: web
    name: blog-post-spa-frontend
    env: static
    branch: main
    buildCommand: echo "No build required"
    envVars:
      - key: API_URL
        sync: false
        value: https://code-challenge-3-1-nqda.onrender.com
